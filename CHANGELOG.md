# Changelog

本仓库包含两个产品：**VS Code 扩展**（`align_symbols_vscode`）与 **Visual Studio 2013 扩展**（`align_symbols_vs2013`）。两者共用同一套"按符号分列对齐"规则，但版本号独立维护并保持一致。以下按版本列出各自的变更。

> 版本号规则：同一功能在两端的版本号保持一致。VS2013 扩展从 **0.9.0** 起才存在（此前只有 VS Code 版）。

## 0.9.0

**VS Code 扩展**

- 扩展 ID 更名为 `align_symbols_vscode`（安装后在 VS Code 中的显示名保持不变）。
- 调用参数为链式下标（`name[s1][s2]`）时，在首个 `[` 处拆分，使名字补齐以对齐第一个下标，其余下标保持粘连（例如 `ch_kelvin[site_no][0]`）。

**VS2013 扩展**

- 首个与 VS Code 同版本的 **0.9.0** 版本；命令文本由 `AlignSelectedCode` 改为 **`Align Selected Code`**（增加空格提升可读性，版本号不升级）。

## 0.8.0

**VS Code 扩展**

- 调用参数本身为 `name[sub]`（如 `vinp_kelvin[site_no]`）时，只填充名字使内部 `[sub]` 对齐；普通参数保持按单元对齐。

**VS2013 扩展**

- 尚未发布（仅 VS Code 版）。

## 0.7.0

**VS Code 扩展**

- 嵌套访问链现在也把 `[` 当作访问符，因此数组赋值 `global_x[a][b] = local_x[b];` 会对齐每个 `[` 前的标识符（保持括号链粘连）。链式下标不再被简单的 `member[sub] = value` 形式误处理。

**VS2013 扩展**

- 尚未发布（仅 VS Code 版）。

## 0.6.0

**VS Code 扩展**

- 调用对齐现在识别前置赋值（`LHS = Func(args);`），同时对齐 `=` 与调用，使诸如 `CParam *x = StsGetParam(...)` 的声明行把 `=` 推到同一列。

**VS2013 扩展**

- 尚未发布（仅 VS Code 版）。

## 0.5.0

**VS Code 扩展**

- 新增通用"同构"对齐器：任意连续若干行若共享同一符号骨架，则逐列对齐（所有符号/运算符），只插入空格。在专用模式之后作为回退。

**VS2013 扩展**

- 尚未发布（仅 VS Code 版）。

## 0.4.0

**VS Code 扩展**

- 新增嵌套成员访问调用链：`OBJ1->Method1(a, b, OBJ2.Method2(c, d));`，按序数对齐每个接收者（`->` / `.` 前的标识符）。

**VS2013 扩展**

- 尚未发布（仅 VS Code 版）。

## 0.3.0

**VS Code 扩展**

- 新增方法/函数调用表支持：`obj.Func(arg1, arg2, ...);`，对齐对象名与各参数列。

**VS2013 扩展**

- 尚未发布（仅 VS Code 版）。

## 0.2.0

**VS Code 扩展**

- 通用化对齐引擎：支持 `member[sub] = value; ...` 表（可带前导块注释）、更多运算符（`|=`、`&=`、`+=`、`...`）以及用 `|` 分隔的位运算和。

**VS2013 扩展**

- 尚未发布（仅 VS Code 版）。

## 0.1.0

**VS Code 扩展**

- 初始发布：链式 bitfield 赋值按列对齐。

**VS2013 扩展**

- 尚未发布（仅 VS Code 版）。
